<UserControl x:Class="Common.NumericUpDownControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Common"
             mc:Ignorable="d" d:DesignHeight="30" d:DesignWidth="100">
    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="txtNum" x:FieldModifier="private" Margin="0,0,0,0" Text="0" TextChanged="txtNum_TextChanged" Grid.Column="0" />
        <Button x:Name="cmdUp" x:FieldModifier="private" Margin="0,0,0,0" Content="˄"  Click="cmdUp_Click" Grid.Column="1"/>
        <Button x:Name="cmdDown" x:FieldModifier="private" Margin="0,0,0,0" Content="˅"  Click="cmdDown_Click" Grid.Column="2"/>
    </Grid>
</UserControl>
